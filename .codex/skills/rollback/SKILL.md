---
name: rollback
description: Use when the user asks to run /rollback or roll back the Netin firmware project to the previous committed version. This skill only calls the local ./rollback.sh script.
---

# Rollback

Run exactly:

```bash
rtk ./rollback.sh
```
