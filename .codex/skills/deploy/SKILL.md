---
name: deploy
description: Use when the user asks to run /deploy or deploy the Netin firmware from this project. This skill only calls the local ./deploy.sh script.
---

# Deploy

Run exactly:

```bash
rtk ./deploy.sh
```
